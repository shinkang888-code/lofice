NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Stirling-PDF 서버 (선택) — Docker 8080 등 자체 호스트 URL
# NEXT_PUBLIC_STIRLING_PDF_URL=http://localhost:8080

# hwpreader / rhwp-studio URL (선택) — @rhwp/editor iframe 대상
# NEXT_PUBLIC_RHWP_STUDIO_URL=https://edwardkim.github.io/rhwp

# PPT Master 갤러리/뷰어 URL (선택)
# NEXT_PUBLIC_PPT_MASTER_URL=https://hugohe3.github.io/ppt-master

# ddddocr API 서버 (선택) — Docker 8000 등 자체 호스트 URL
# pip install ddddocr && python -m ddddocr api --host 0.0.0.0 --port 8000
# NEXT_PUBLIC_DDDDOCR_URL=http://localhost:8000

# hwpx-skill API 서버 (선택) — 한글 AI 생성·변환·편집
# git clone https://github.com/jkf87/hwpx-skill && services/hwpx-skill-api/README.md 참고
# NEXT_PUBLIC_HWPX_SKILL_URL=http://localhost:8100
